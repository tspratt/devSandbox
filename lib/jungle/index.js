/**
 * Created by Tracy on 10/14/2015.
 */
/**
 * Created by TracyWork on 4/3/15.
 */
"use strict";

/*


var Animal = function (sType){
  this.energyLevel = 0;
  this.counts = 0;
  this.type = sType;
  this.makeSound = function(){
    if (sType === 'monkey') {
      this.energyLevel -= 4;
    }
    else {
      this.energyLevel -= 3;
    }
  };
  this.eat = function(food) {
    if (sType === 'monkey') {
      this.energyLevel += 2;
    }
    else if (sType === 'tiger') {
      if (food !== 'grain') {
        this.energyLevel += 5;
      }
    }
    else {
      this.energyLevel += 5;
    }
  };
  this.sleep = function () {
    if (sType === 'tiger') {
      this.energyLevel += 5;
    }
    else {
      this.energyLevel += 10;
    }
  }
};




var Tiger = function () {
  var tiger = new Animal('tiger');
  jungle.addAnimal(tiger);
  return tiger
};


var Monkey = function () {
  var monkey = new Animal('monkey');
  jungle.addAnimal(monkey);
  Animal.prototype.play = function() {
    if (monkey.energyLevel >= 8) {
      monkey.energyLevel = monkey.energyLevel - 8
      return 'Oooo Oooo Oooo';
    }
    else {
      return 'Monkey is too tired';
    }

  }
};

var Snake = function () {
  var snake = new Animal('snake');
  jungle.addAnimal(tiger);
  return snake;
};


var jungle = new Jungle();
var monkey = new Monkey();
var tiger = new Tiger();
var snake = new Snake();


tiger.eat(jungle.food.grain);
monkey.eat(jungle.food.fish);
jungle.soundOff();
*/
